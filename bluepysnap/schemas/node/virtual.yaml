title: Nodes - virtual
description: schema for virtual node types
properties:
  nodes:
    allOf: [{ $ref: "#/$node_file_defs/nodes" }]
    patternProperties:
      "":
        properties:
          "0":
            required:
              - model_template
              - model_type
